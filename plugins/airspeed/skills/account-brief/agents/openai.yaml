interface:
  display_name: "Account brief"
  short_description: "Prepare an account brief from Airspeed calls"
  default_prompt: "Use $account-brief to prepare me for a customer conversation."

dependencies:
  tools:
    - type: "mcp"
      value: "airspeed"
      description: "Read accessible Airspeed calls and analysis."
      transport: "streamable_http"
      url: "https://mcp.goairspeed.com/mcp-oauth"
